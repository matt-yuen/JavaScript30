recognition.interimResults - shows intermediate word results

need to add event listener that listens to the end event because after the recognition is complete (isFinal = true), the result listener no longer runs